# Source drawings

These are the three original hand drawings. They are source DNA, not decorative assets.

Current files are photographs of the original drawings. Camera EXIF rotation is baked in by `pnpm process:artworks` so the pixels themselves are right-side up. Replace them with original scans (same filenames), then run that command again.
